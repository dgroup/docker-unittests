#
# Check https://circleci.com/docs/2.0/language-java for more details
#
dependencies:
  override:
    - mvn -DskipTests clean install dependency:resolve-plugins dependency:go-offline -Pqulice

test:
  override:
    - mvn test -Pqulice

notify:
  webhooks:
    - url: https://webhooks.gitter.im/e/5368aea37d6ab0e42bd6
