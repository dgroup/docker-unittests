#
# Check https://circleci.com/docs/2.0/language-java for more details
#
dependencies:
  override:
    - mvn -DskipTests clean install dependency:resolve-plugins dependency:go-offline

test:
  override:
     - mvn -Psonar sonar:sonar \
        -Dsonar.organization=dgroup-github \
        -Dsonar.host.url=https://sonarcloud.io \
        -Dsonar.login=9006f420405fb08ec8373a62083221c9c3f83fc0
    - mvn test -Pqulice && bash <(curl -s https://codecov.io/bash)

notify:
  webhooks:
    - url: https://webhooks.gitter.im/e/5368aea37d6ab0e42bd6
