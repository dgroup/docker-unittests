architect:
  - dgroup

readers:
  - "urn:github:1651114"

assets:
  secret.xml: "dgroup/mvn-secret-settings#docker-unittests.xml"

merge:
  script:
    - mvn -Pqulice -Ddocker_installed=no clean package

#deploy:
#  env:
#    MAVEN_OPTS: "-XX:MaxPermSize=256m -Xmx1g"
#  script:
#    - "sudo bundle"
#    - "mvn clean deploy -Prultor --settings ../settings.xml"
#    - "mvn clean site-deploy -Psite -Prempl --settings ../settings.xml"

release:
  script: |
    mvn versions:set "-DnewVersion=$tag"
    git commit -am "$tag"
    mvn -Pqulice -Ddocker_installed=no clean package
    mvn -Prelease -s secret.xml github-release:release